# College Football Legends - Next Build Plan

Date: 2026-05-11

This root plan is mirrored at `../NEXT_BUILD_PLAN.md` for easy access.

The current decision is:

- `cfl-next/` is the active visual/reference prototype.
- `cfl-app/` should be the new React + TypeScript + Vite real app path.
- `cfl_full_scale_seed_data_v0_1/` is the first real seed source.
- legacy root `app.js`, `js/sim/`, `js/ui/`, `harness/`, and old docs are parts bin/reference.

The first coding milestone:

```text
Load real seed data, select one program, render its 105-player roster from normalized records, click a player, show real profile, save selected career.
```

See the full root file:

```text
NEXT_BUILD_PLAN.md
```
